<p id="notice"><%= notice %></p>

<p>
  <b>Full name:</b>
  <%= @member.full_name %>
</p>

<p>
  <b>Instrument:</b>
  <%= @member.instrument %>
</p>


<%= link_to 'Edit', edit_band_member_path(@member.band, @member) %> |

<%= link_to 'New Member', new_band_member_path(params[:band_id])%> |
<%= link_to 'See Band Members', band_members_path(params[:band_id]) %>
